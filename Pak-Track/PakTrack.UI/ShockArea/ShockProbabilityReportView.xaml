<UserControl x:Class="PakTrack.UI.ShockArea.ShockProbabilityReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             xmlns:oxy="http://oxyplot.org/wpf"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
  
        <ScrollViewer>
            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

            <oxy:PlotView Controller ="{Binding HoverController}"  Grid.Row="0" Model="{Binding ModelX}"  Height="500" />
            <oxy:PlotView Controller ="{Binding HoverController}" Grid.Row="1" Model="{Binding ModelY}" Height="500"/>
            <oxy:PlotView Controller ="{Binding HoverController}" Grid.Row="2" Model="{Binding ModelZ}" Height="500"/>
        </Grid>

    </ScrollViewer>
</UserControl>

