<UserControl x:Class="PakTrack.UI.VibrationArea.ConsolidatedReportViews.VectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PakTrack.UI.VibrationArea.ConsolidatedReportViews"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListView ItemsSource="{Binding ReportDetails.Vector}" Grid.Column="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Frequency(Hz)" DisplayMemberBinding="{Binding Frequency}" />
                    <GridViewColumn  Header="PSD, g²/Hz" DisplayMemberBinding="{Binding Value}" />
                </GridView>
            </ListView.View>
        </ListView>
        <oxy:PlotView Grid.Column="1" Model="{Binding VectorPlotModel}" MaxHeight="600" MinHeight="400"/>
    </Grid>
</UserControl>
