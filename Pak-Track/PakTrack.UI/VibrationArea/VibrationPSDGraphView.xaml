<UserControl x:Class="PakTrack.UI.VibrationArea.VibrationPSDGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="TextBlock" Margin="20,0,0,0" FontSize="25" TextWrapping="Wrap"
                           Text="{Binding Title}" Grid.Column="0" />

                <Button  Command="{Binding GoBackCommand}" Grid.Column="1" HorizontalAlignment="Right"
                         Style="{StaticResource PakTrackAccentedSquareButtonStyle}" VerticalAlignment="Center" Margin="0,0,15,0">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="15"
                                   Height="15"
                                   Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_arrow_left}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                        <TextBlock Margin="4 0 0 0"
                                   VerticalAlignment="Center"
                                   Text="Go Back" />
                    </StackPanel>
                </Button>
            </Grid>
        </StackPanel>

       


        <xctk:BusyIndicator IsBusy="{Binding IsBusy}" BusyContent="Plotting Chart..." Grid.Row="2">
            <wpf:CartesianChart Series="{Binding SeriesCollection}" DisableAnimations="True" LegendLocation="Bottom">
                <wpf:CartesianChart.AxisY>
                    <wpf:Axis Title="G²/Hz" FontSize="18" />
                </wpf:CartesianChart.AxisY>
                <wpf:CartesianChart.AxisX>
                    <wpf:Axis Title="Frequency (Hz)" FontSize="18" />
                </wpf:CartesianChart.AxisX>
            </wpf:CartesianChart>
        </xctk:BusyIndicator>
    </Grid>
</UserControl>



